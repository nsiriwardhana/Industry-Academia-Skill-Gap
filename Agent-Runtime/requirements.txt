fastapi>=0.104.0
uvicorn[standard]>=0.24.0
neo4j>=5.14.0
pydantic>=2.0.0
python-dotenv>=1.0.0
requests>=2.31.0
urllib3>=2.0.0  # For SSL retry handling
python-multipart>=0.0.6  # For file uploads

# Image processing
Pillow>=10.0.0

# OCR (fallback when HuggingFace unavailable)
easyocr>=1.7.0

# PDF processing (optional - for PDF support)
pdf2image>=1.16.0
PyPDF2>=3.0.0
pdfplumber>=0.10.0  # Fast PDF text extraction

# LLM Integration for CV Parsing
openai>=1.0.0  # For Open Router API (uses OpenAI SDK format)
google-genai>=0.3.0  # For Gemini API (new package)

# XAI dependencies
shap>=0.43.0
joblib>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# AI Explainer - Fine-tuned Qwen model
torch>=2.0.0
transformers>=4.36.0
peft>=0.7.0  # For LoRA adapter loading
accelerate>=0.25.0  # For device mapping
